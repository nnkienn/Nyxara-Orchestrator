import type { BenchmarkConfig } from "./scenario.types.js";
export function validateConfig(config: BenchmarkConfig): BenchmarkConfig { if (!Number.isInteger(config.warmupRuns) || config.warmupRuns < 0) throw new Error("warmupRuns must be a non-negative integer"); if (!Number.isInteger(config.measuredRuns) || config.measuredRuns < 1) throw new Error("measuredRuns must be positive"); if (config.sampleIntervalMs < 100 || config.sampleIntervalMs > 2000) throw new Error("sampleIntervalMs must be between 100 and 2000"); if (config.longRunWorkflows < 1) throw new Error("longRunWorkflows must be positive"); return config; }
