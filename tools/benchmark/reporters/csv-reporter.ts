import fs from "node:fs/promises";
import type { BenchmarkReport } from "../scenario.types.js";
export async function writeCsvReport(file: string, report: BenchmarkReport) { const h = "scenario,workload_profile,repo_class,tasks,runs,duration_median_ms,duration_p95_ms,harness_duration_median_ms,rss_baseline_mb,rss_peak_mb,process_tree_rss_mb,rss_final_mb,rss_retained_mb,input_tokens,output_tokens,provider_calls,tool_calls,repair_cycles,latency_source,memory_source,token_source\n"; const rows = report.scenarios.map(s => { const m = s.memory; const x = s.metrics; return [s.name, s.workloadProfile ?? "", report.repository.classification ?? "", "", s.runs.length, s.duration?.median ?? "", s.duration?.p95 ?? "", s.harnessDuration?.median ?? "", m?.baselineRssMb ?? "", m?.peakRssMb ?? "", m?.processTreeRssMb ?? "", m?.finalRssMb ?? "", m?.retainedRssMb ?? "", x ? Object.values(x.tokens).reduce((n, t) => n + (t.input ?? 0), 0) : "", x ? Object.values(x.tokens).reduce((n, t) => n + (t.output ?? 0), 0) : "", x?.providerCalls ?? "", x?.toolCalls ?? "", x?.repairCycles ?? "", s.latencySource ?? "", m?.memorySource ?? "", x?.tokenSource ?? ""].join(","); }).join("\n"); await fs.writeFile(file, h + rows + (rows ? "\n" : "")); }
