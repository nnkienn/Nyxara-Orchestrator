import { describe, expect, it } from "vitest";
import fs from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { writeJsonReport } from "../reporters/json-reporter.js";
import { writeCsvReport } from "../reporters/csv-reporter.js";
import { writeMarkdownReport } from "../reporters/markdown-reporter.js";
describe("report generation", () => {
  it("writes valid JSON/CSV/Markdown without secrets", async () => { const d = await fs.mkdtemp(path.join(os.tmpdir(), "nyxara-bench-")); const report: any = { schemaVersion: 1, benchmarkRunId: "x", timestamp: new Date().toISOString(), environment: { os: "linux" }, repository: { classification: "small", sourceFileCount: 1 }, configuration: {}, scenarios: [{ name: "idle", status: "completed", runs: [], duration: { median: 1, p95: 2 }, memory: { peakRssMb: 3 } }], summary: {}, warnings: [], secret: "API_KEY_SHOULD_NOT_APPEAR" }; await writeJsonReport(path.join(d, "report.json"), report); await writeCsvReport(path.join(d, "report.csv"), report); await writeMarkdownReport(path.join(d, "report.md"), report); const j = await fs.readFile(path.join(d, "report.json"), "utf8"); expect(JSON.parse(j).schemaVersion).toBe(1); expect(await fs.readFile(path.join(d, "report.csv"), "utf8")).toContain("scenario"); expect(await fs.readFile(path.join(d, "report.md"), "utf8")).toContain("Nyxara Local Benchmark"); });
  it("normalizes non-finite values and selects only normal product baselines", async () => { const d = await fs.mkdtemp(path.join(os.tmpdir(), "nyxara-bench-")); const report: any = { schemaVersion: 1, benchmarkRunId: "x", timestamp: "x", environment: {}, repository: {}, configuration: { providerMode: "fake", workloadProfile: "heavy" }, summary: {}, warnings: [], scenarios: [{ name: "workflow", workloadProfile: "heavy", status: "completed", runs: [], duration: { median: Infinity, p95: NaN }, memory: { peakRssMb: 999, processTreeRssMb: NaN } }, { name: "workflow", workloadProfile: "normal", status: "completed", runs: [], duration: { median: 2, p95: 3 }, memory: { peakRssMb: 42, stabilizedRssMb: 40 } }] }; await writeJsonReport(path.join(d, "r.json"), report); await writeCsvReport(path.join(d, "r.csv"), report); await writeMarkdownReport(path.join(d, "r.md"), report); const json = await fs.readFile(path.join(d, "r.json"), "utf8"); const csv = await fs.readFile(path.join(d, "r.csv"), "utf8"); const md = await fs.readFile(path.join(d, "r.md"), "utf8"); expect(json).not.toMatch(/NaN|Infinity/); expect(JSON.parse(json).scenarios[0].duration.median).toBeNull(); expect(csv).not.toMatch(/NaN|Infinity/); expect(md).not.toMatch(/NaN|Infinity/); expect(md).toContain("Normal workflow peak: 42.00 MB"); expect(md).toContain("Current Stress Profile — Heavy"); });
});
